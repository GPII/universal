This configuration file is used by the for acceptance test: windows-virtualMagnifyingGlass-testSpec.js.

It includes to the basic localInstall setup for acceptance tests (modifying the preferences folder used), which in turn includes the standard development config file (running GPII locally, using development setup).

This config sets the device reporter file to be 'virtualMagnifyingGlass.json', which will report Virutal Magnifying Glass as being installed on the system.