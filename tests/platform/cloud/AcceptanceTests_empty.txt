AcceptanceTests_empty.js

Description:
This is a quick test to ensure that the system doesn't explode when attempting to login with an empty preference set (see GPII-668). It uses the empty.json preference set and the system configured as a cloudbased flowmanager.

Requirements:
None