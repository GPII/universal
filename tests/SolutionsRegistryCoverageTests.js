// TODO:  Write this

// Adapted from https://github.com/istanbuljs/istanbuljs/blob/c1f27a6d243dd348ab8c21806ec73a95baf02b3e/packages/istanbul-lib-report/lib/file-writer.js#L98

// TODO: Ensure that a schema is present for all entries (this may need to be a warning at first).


// TODO: Coloured warnings on missing schema data, exit with an error if there is invalid scheme data.

// TODO: Test everything matching %gpii-universal/testData/solutions/*.json(5)


/*

ConsoleWriter.prototype.colorize = function (str, clazz) {
    var colors = {
        low: '31;1',
        medium: '33;1',
        high: '32;1'
    };

if (supportsColor.stdout && colors[clazz]) {
    return '\u001b[' + colors[clazz] + 'm' + str + '\u001b[0m';
}
return str;
};

 */
