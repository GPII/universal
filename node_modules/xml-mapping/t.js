var XMLWriter = require('xml-writer');
var XMLMapping = require('./');
var input;
var output;

input = {};
input = { 
	key : {}
};
input = {
	key1 : {},
	key2 : {}
};
input = { 
	key :[] 
};
input = {
	key : [{ $t : 'value'}, { $t : 'value'}]
};

output = XMLMapping.dump(input);
console.log(output);
