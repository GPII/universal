{
    "type": "gpii.tests.preferencesServer.config",
    "options": {
        "gradeNames": ["fluid.component"],
        "distributeOptions": {
            "preferencesServerType": {
                "record": "gpii.tests.preferencesServer",
                "target": "{that preferencesServer}.type"
            }
        }
    },
    "mergeConfigs": [
        "%preferencesServer/configs/gpii.preferencesServer.config.base.json5"
    ]
}
