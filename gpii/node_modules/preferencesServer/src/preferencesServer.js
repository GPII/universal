/*!
GPII Preferences Server

Copyright 2012 OCAD University

Licensed under the New BSD license. You may not use this file except in
compliance with this License.

You may obtain a copy of the License at
https://github.com/gpii/universal/LICENSE.txt
*/

(function () {

    "use strict";

    var fluid = require("infusion"),
        path = require("path"),
        gpii = fluid.registerNamespace("gpii");

    fluid.require("gpiiFramework", require);
    fluid.require("./userGet.js", require);
    fluid.require("./userPost.js", require);
    
    fluid.defaults("gpii.preferencesServer", {
        gradeNames: ["gpii.app", "autoInit"],
        handlers: {
            userGet: {
                route: "/user/:token",
                type: "get"
            },
            userPost: {
                route: "/user/:token",
                type: "post"
            }
        },
        userSourceUrl: "",
        components: {
            rawUserSource: {
                type: "gpii.dataSource",
                options: {
                    url: "{gpii.preferencesServer}.options.userSourceUrl",
                    writable: true,
                    termMap: {
                        token: "%token"
                    }
                }
            },
            userSource: {
                type: "gpii.callbackWrappingDataSource"
            }
        }
    });

    fluid.demands("gpii.urlExpander", ["preferencesServer.development", "gpii.preferencesServer"], {
        options: {
            vars: {
                port: "{gpii.server}.options.port",
                root: path.join(__dirname, "..")
            }
        }
    });

    fluid.demands("rawUserSource", "gpii.preferencesServer", {
        funcName: "gpii.dataSource.CouchDB"
    });

    fluid.demands("rawUserSource", ["gpii.preferencesServer", "preferencesServer.development"], {
        funcName: "gpii.dataSource.URL"
    });

    fluid.demands("userSource", "gpii.preferencesServer", {
        options: {
            components: {
                rawSource: "{rawUserSource}"
            }
        }
    });
    
})();