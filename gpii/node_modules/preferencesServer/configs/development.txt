development.all.local.json
==========================

This configuration runs the preferences server locally and in development mode.

It expects that:
* The raw preferences server is running on the same port on the local machine