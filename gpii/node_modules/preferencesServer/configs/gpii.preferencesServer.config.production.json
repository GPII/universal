{
    "type": "gpii.preferencesServer.config.production",
    "distributeOptions": {
        "userSourceUrl": {
            "record": "http://localhost:5984/user/%userToken",
            "target": "{that preferencesServer}.options.userSourceUrl"
        }
    },
    "includes": [
        "./gpii.preferencesServer.config.base.json"
    ]
}
