{
    "typeName": "preferencesServer.development",
    "options": {
        "gradeNames": ["autoInit", "fluid.littleComponent"],
        "distributeOptions": [{
            "source": "{that}.options.devUrls",
            "target": "{that preferencesServer}.options.urls"
        }],
        "devUrls": {
            "rawPreferences": "http://localhost:%port/rawPreferences/%userToken"
        },
        "components": {
            "server": {
                "type": "kettle.server",
                "options": {
                    "logging": true,
                    "components": {
                        "preferencesServer": {
                            "type": "gpii.preferencesServer",
                            "options": {
                                "gradeNames": ["gpii.preferencesServer.dev"]
                            }
                        }
                    }
                }
            }
        }
    },
    "includes": [
        "./base.json"
    ]
}