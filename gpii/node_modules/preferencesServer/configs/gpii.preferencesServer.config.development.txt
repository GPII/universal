gpii.preferencesServer.config.development.json
==============================================

This configuration runs the preferences server in development mode.

It expects that:
* The pouchDB is running and has data loaded
