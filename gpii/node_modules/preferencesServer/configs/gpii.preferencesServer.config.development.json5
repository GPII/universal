/**
 * This configuration runs the preferences server locally and in development mode.
 */
{
    "type": "gpii.preferencesServer.config.development",
    "mergeConfigs": [
        "./gpii.preferencesServer.config.base.json5"
    ]
}
