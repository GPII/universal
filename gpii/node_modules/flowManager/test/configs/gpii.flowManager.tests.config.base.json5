{
    "type": "gpii.flowManager.tests.config.base",
    "mergeConfigs": [
    // This is absolutely absurd, how can a test config for the flowManager inherit from the whole architecture
        "%gpii-universal/gpii/configs/gpii.config.development.all.local.json"
    ]
}
