// The "generic" schema for settings payloads, which only naively represents individual settings.  Used as the base
// for the generated settings schema found in the build directory.
{
    "$schema": "gss-v7-full#",
    "type": "object",
    "propertyNames": {
        "type": "string",
        "format": "uri"
    },
    "properties": {
    }
}
