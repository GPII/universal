/*
GPII Preferences Server GET Handler

Copyright 2012 OCAD University

Licensed under the New BSD license. You may not use this file except in
compliance with this License.

You may obtain a copy of the License at
https://github.com/gpii/universal/LICENSE.txt
*/

(function () {

    "use strict";

    var fluid = require("infusion"),
        gpii = fluid.registerNamespace("gpii");

    fluid.demands("gpii.requests.request.handler", "solutionsQuery", {
        options: {
            invokers: {
                handle: {
                    funcName: "gpii.requests.request.handler.solutionsQuery",
                    args: ["{request}", "{solutionsRegistry}.solutionsQuerySource"]
                }
            }
        }
    });

    gpii.requests.request.handler.solutionsQuery = function (request, solutionsQuerySource) {
        var req = request.req,
            callback = gpii.requests.request.handler.makeCallback(request.res);
        if (!req.query.os) {
            callback({
                isError: true,
                message: "No os specified."
            });
            return;
        }
        solutionsQuerySource.get({
            os: req.query.os
        }, callback);
    };

})();