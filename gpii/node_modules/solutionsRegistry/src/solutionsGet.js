/*!
GPII Preferences Server GET Handler

Copyright 2012 OCAD University

Licensed under the New BSD license. You may not use this file except in
compliance with this License.

You may obtain a copy of the License at
https://github.com/gpii/universal/LICENSE.txt
*/

(function () {

    "use strict";

    var fluid = require("infusion"),
        gpii = fluid.registerNamespace("gpii");

    fluid.demands("gpii.requests.request.handler", "solutionsGet", {
        options: {
            invokers: {
                handle: {
                    funcName: "gpii.requests.request.handler.solutionsGet",
                    args: ["{request}", "{solutionsRegistry}.solutionsSource"]
                }
            }
        }
    });

    gpii.requests.request.handler.solutionsGet = function (request, solutionsSource) {
        solutionsSource.get({
            id: request.req.params.id
        }, gpii.requests.request.handler.makeCallback(request.res));
    };

    gpii.requests.request.handler.makeCallback = function (res) {
        return function (resp) {
            if (!resp) {
                res.send({
                    isError: true,
                    message: "Unknown error"
                }, 500);
                return;
            }
            if (resp.isError) {
                res.send(resp, 500);
                return;
            }
            res.send(resp, 200);
        };
    };

})();