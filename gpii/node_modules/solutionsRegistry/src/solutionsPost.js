/*!
GPII Preferences Server POST Handler

Copyright 2012 OCAD University

Licensed under the New BSD license. You may not use this file except in
compliance with this License.

You may obtain a copy of the License at
https://github.com/gpii/universal/LICENSE.txt
*/

(function () {

    "use strict";

    var fluid = require("infusion"),
        gpii = fluid.registerNamespace("gpii");

    fluid.demands("gpii.requests.request.handler", "solutionsPost", {
        options: {
            invokers: {
                handle: {
                    funcName: "gpii.requests.request.handler.solutionsPost",
                    args: ["{request}", "{solutionsRegistry}.solutionsSource"]
                }
            }
        }
    });

    gpii.requests.request.handler.solutionsPost = function (request, solutionsSource) {
        var req = request.req;
        solutionsSource.set({
            id: req.params.id
        }, req.body, gpii.requests.request.handler.makePostCallback(request.res));
    };

    gpii.requests.request.handler.makePostCallback = function (res) {
        return function (resp) {
            if (resp.ok) {
                resp = fluid.model.transformWithRules(resp, {
                    "_rev": "rev",
                    "_id": "id"
                });
            }
            var callback = gpii.requests.request.handler.makeCallback(res);
            callback(resp);
        };
    };

})();