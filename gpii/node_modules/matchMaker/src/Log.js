var fluid = require('fs');

var log = fluid.registerNamespace("gpii.matchMaker.log");

log.inAutoFile = function (preferences, suffix) {
	var curDate = new Date();
	fs.writeFile(path.resolve(__dirname, "/logs/" + curDate.getTime() + "_" + suffix + ".json"), preferences, function(err) {
		if(err) {
			console.log(err);
		} else {
			console.log("The file was saved!");
		}
	});
};
