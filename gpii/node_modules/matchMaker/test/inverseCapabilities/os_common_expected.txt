No output is expected for this test, since the preferences file contains only common terms.
Whilst these are correctly ontologised into hierarchical form by the ontology server,
the resulting preferences set contains no "applications" block and so images to nothing
by the inverse capabilities rules.