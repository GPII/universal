{
    "type": "gpii.tests.rawPreferencesServer.config",
    "options": {
        "distributeOptions": {
             "serverPort": {
                 "record": 8078,
                 "target": "{that server}.options.port"
             },
             "rawPreferencesDataSourcePath": {
                 "record": "%rawPreferencesServer/test/data/%userToken.json",
                 "target": "{that rawPreferencesServer rawPreferencesDataSource}.options.path"
             }
        }
    },
    "mergeConfigs": [
        "%rawPreferencesServer/configs/gpii.rawPreferencesServer.config.development.json"
    ]
}
