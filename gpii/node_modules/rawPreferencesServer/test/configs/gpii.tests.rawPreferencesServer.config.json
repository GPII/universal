{
    "type": "gpii.tests.rawPreferencesServer.config",
    "options": {
        "distributeOptions": {
             "serverPort": {
                 "record": 8078,
                 "target": "{that server}.options.port"
             },
             "rawPreferencesSourceUrl": {
                 "record": "file://%rawPreferencesServer/test/%userToken.json",
                 "target": "{that rawPreferencesSource}.options.url"
             }
        }
    },
    "includes": [
        "%rawPreferencesServer/configs/gpii.rawPreferencesServer.config.development.json"
    ]
}
